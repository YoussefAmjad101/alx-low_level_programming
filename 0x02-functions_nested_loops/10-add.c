#include "main.h"
/**
 * add - my program
 * Description: adds two integers and returns the result.
 * @nbr1: variable that stocks the first number to be add.
 * @nbr2: variable that stocks the second number to be add.
 * Return: Return the result.>_<
 */
int add(int a, int b) {
    return a + b;
}

int main() {
   int n;
   n = add(89, 9);
   printf("%d\n", n);
  return 0;
}
